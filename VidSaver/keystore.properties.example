# Copy to `keystore.properties` (git-ignored) to sign release builds.
# When this file is absent, `assembleRelease` still succeeds but produces an
# unsigned APK, so a fresh clone builds without any secrets.

storeFile=../vidsaver-release.jks
storePassword=
keyAlias=vidsaver
keyPassword=
